{
  "name": "smart-retail-pos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:app": "npm --prefix desktop-next install",
    "dev": "npm --prefix desktop-next run dev",
    "check": "npm --prefix desktop-next run check",
    "test": "npm --prefix desktop-next run test",
    "build": "npm --prefix desktop-next run build",
    "start": "npm --prefix desktop-next run start",
    "dist": "npm --prefix desktop-next run dist"
  }
}
